export const SET_TOKEN = (state, val) => {
    state.token = val
}

export const SET_USER_DATA = (state, val) => {
    state.user = val
}

export const SET_LIST_USERS = (state, val) => {
    state.users = val
}
