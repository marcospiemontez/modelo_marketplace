{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: lint",
            "request": "launch",
            "command": "yarn run lint",
            "cwd": "${workspaceFolder}"
        }
    ]
}